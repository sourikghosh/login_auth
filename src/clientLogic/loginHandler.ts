//axios post req to "api/login"
//and set the header with the jwt from response
import axios from 'axios'
//idk how to perform that
export const loginHandler = async () => {
    //axios.post('api/login')
    //if success : render login.ejs
    //else render error.ejs
}

//my doubt is that how to add this handler to by login/signup BUTTON ??
//and how does it works if 